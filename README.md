# 实验课器材预约与损耗结转系统

本地启动即可运行，含样例课程、班级和库存数据，支持完整器材流转流程。

## 快速启动

```bash
npm install
npm start
```

浏览器打开 http://localhost:3001

## 预置账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员（全部权限） |
| zhangsan | 123456 | 教师（张三·物理） |
| lisi | 123456 | 教师（李四·化学） |
| wangwu | 123456 | 实验员（审批+结转） |

## 预置数据

- **课程**: 大学物理实验（张三）、有机化学实验（李四）、生物基础实验（张三）
- **班级**: 物理实验A班、物理实验B班、化学实验A班
- **器材**: 示波器(10)、万用表(5)、烧杯(50)、显微镜(3)、试管(100)

---

## 主流程演示

以下步骤演示一门课预约器材 → 教师领用 → 归还部分 → 提交损耗 → 审批通过 → 周结转的完整流程。

### 步骤 1: 教师预约器材

1. 登录 `zhangsan / 123456`
2. 点击「器材预约」→ 点击「+ 新建预约」
3. 选择课程「大学物理实验」、班级「物理实验A班」、器材「显微镜」、数量 `2`、周次 `2026-W25`
4. 提交 → 预约创建成功，显微镜可用数从 3 减为 1（锁定 2）

### 步骤 2: 实验员审批预约

1. 退出登录，登录 `wangwu / 123456`
2. 在「器材预约」页面，找到状态为「待审批」的预约，点击「审批」
3. 预约状态变为「已审批」

### 步骤 3: 教师领用器材

1. 退出登录，登录 `zhangsan / 123456`
2. 在「器材预约」页面，找到状态为「已审批」的预约，点击「领用」
3. 预约状态变为「已领用」

### 步骤 4: 归还部分器材

1. 仍在「器材预约」页面，点击「归还」
2. 归还数量输入 `1`（部分归还）
3. 预约状态变为「部分归还」，显微镜可用数 +1

### 步骤 5: 申报损耗

1. 点击「损耗申报」→ 点击「+ 申报损耗」
2. 选择关联预约（显微镜预约），损耗数量 `1`，原因填写「镜头摔碎」
3. 提交成功，损耗记录状态为「待审批」

### 步骤 6: 审批损耗

1. 退出登录，登录 `wangwu / 123456`
2. 在「损耗申报」页面，找到待审批记录，点击「通过」
3. 审批通过，显微镜总量从 3 减为 2，锁定数从 1 减为 0

### 步骤 7: 执行周结转

1. 仍在 `wangwu` 账号，点击「周结转」→ 点击「执行周结转」
2. 周次填 `2026-W25`（与预约周次一致），确认
3. 结转成功，展示器材快照、预约汇总、损耗汇总和待归还状态
4. 点击「导出结转数据」可下载 JSON 文件

### 步骤 8: 验证持久化

1. 在终端按 `Ctrl+C` 停止服务器
2. 再次运行 `npm start`
3. 登录后，器材库存、预约记录、损耗记录、结转数据均保留

---

## 失败路径演示

### 失败路径 1: 两门课抢同一低库存器材

**目的**: 验证库存冲突检测

1. 登录 `zhangsan / 123456`，预约显微镜 2 个（可用 3 → 锁定 2，可用 1）
2. 退出登录，登录 `lisi / 123456`
3. 尝试预约显微镜 2 个 → 系统提示「库存不足：显微镜可用 1，需 2」，预约被拒绝
4. 在「操作日志」中可看到 `reservation_conflict` 记录

### 失败路径 2: 教师审批自己提交的损耗

**目的**: 验证自我审批禁止

1. 登录 `zhangsan / 123456`，完成预约→审批→领用流程
2. 提交一条损耗申报
3. 退出登录，再以 `zhangsan` 登录（教师角色无审批按钮，此路径需通过 API 模拟）
4. 或用管理员 `admin` 账号尝试审批自己提交的损耗 → 系统提示「不能审批自己提交的损耗申报」
5. 在「操作日志」中可看到 `self_approve_rejected` 记录

### 失败路径 3: 同一周重复执行结转

**目的**: 验证重复结转禁止

1. 登录 `wangwu / 123456`，执行 `2026-W25` 的周结转
2. 再次点击「执行周结转」，输入相同周次 `2026-W25`
3. 系统提示「2026-W25 已完成结转，不可重复执行」
4. 在「操作日志」中可看到 `duplicate_settlement_rejected` 记录

---

## 角色权限说明

| 操作 | 教师 | 实验员 | 管理员 |
|------|------|--------|--------|
| 创建预约 | ✅（仅自己课程） | ❌ | ✅ |
| 审批预约 | ❌ | ✅ | ✅ |
| 领用器材 | ✅（仅自己课程） | ❌ | ✅ |
| 归还器材 | ✅（仅自己课程） | ❌ | ✅ |
| 申报损耗 | ✅ | ❌ | ✅ |
| 审批损耗 | ❌ | ✅（不能审批自己） | ✅（不能审批自己） |
| 执行周结转 | ❌ | ✅ | ✅ |
| 调整库存总量 | ❌ | ✅ | ✅ |
| 查看日志 | ✅ | ✅ | ✅ |
| 导出结转数据 | ✅ | ✅ | ✅ |

## 技术架构

- **后端**: Node.js + Express
- **数据库**: SQLite (sql.js，纯 JS 实现，数据文件 `data/lab.db`)
- **前端**: 原生 HTML/CSS/JS 单页应用
- **持久化**: 所有写操作即时落盘，重启后数据完整恢复

## 重置数据

在页面底部点击「重置数据」按钮，或删除 `data/lab.db` 文件后重启服务器。
